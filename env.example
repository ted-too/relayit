# RelayIt Configuration
# Copy this file to .env and fill in real values before `docker compose up`.
# Required vars match apps/api server + shared env schemas and apps/web runtime.

# =======================
# Images
# =======================
# ghcr.io/ted-too/relayit-{api,web} — see https://github.com/ted-too/relayit/releases
IMAGE_TAG=alpha

# =======================
# Public URLs
# =======================
# API_URL → api process + web VITE_API_URL
# WEB_URL → api APP_URL + web VITE_BASE_URL
API_URL=http://localhost:3005
WEB_URL=http://localhost:3000

# =======================
# API process
# =======================
LOG_LEVEL=info

# =======================
# Database (Postgres 18 in compose)
# =======================
POSTGRES_USER=postgres
POSTGRES_PASSWORD=your-secure-password-here
POSTGRES_DB=relayit
# Override if not using the compose postgres service:
# DATABASE_URL=postgres://user:pass@host:5432/relayit?sslmode=require

# =======================
# Redis
# =======================
REDIS_URL=redis://redis:6379

# =======================
# Auth
# =======================
# openssl rand -base64 32
BETTER_AUTH_SECRET=your-very-secure-secret-key-32-chars-min

# =======================
# Credential encryption
# =======================
# openssl rand -hex 32
ENCRYPTION_KEY_VERSION=v1
CREDENTIAL_ENCRYPTION_KEY_V1=your-64-character-hex-encryption-key-here

# =======================
# Object storage (S3-compatible, e.g. R2)
# =======================
S3_ENDPOINT=https://your-account.r2.cloudflarestorage.com
S3_REGION=auto
S3_ACCESS_KEY_ID=your-s3-access-key-id
S3_SECRET_ACCESS_KEY=your-s3-secret-access-key
S3_BUCKET=relayit
