{
  "name": "@repo/api",
  "exports": {
    ".": "./src/trpc/router.ts"
  },
  "scripts": {
    "dev": "NODE_ENV=development bun run --hot src/index.ts",
    "build": "bun build ./src/index.ts --outdir ./build --target bun; cp -r ./drizzle ./build/drizzle;",
    "db:studio": "drizzle-kit studio",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "drizzle-kit migrate"
  },
  "dependencies": {
    "@repo/shared": "workspace:*",
    "better-auth": "^1.3.9",
    "better-auth-harmony": "^1.2.5",
    "drizzle-orm": "^0.44.5",
    "hono": "^4.9.6",
    "pg": "^8.16.3",
    "pino": "^9.9.4",
    "slugify": "^1.6.6"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/pg": "^8.15.5",
    "drizzle-kit": "^0.31.4"
  }
}
